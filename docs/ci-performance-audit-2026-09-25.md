# CI performance audit (2026-09-25)

Work in progress. This file exists on a measurement branch only, so that
baseline CI runs are dispatched on a commit that is never a production
candidate. The finished audit lands on main separately.
